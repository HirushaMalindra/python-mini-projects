# PDF2Image-converter
A lightweight Python utility built with PyMuPDF to cleanly convert PDF pages into individual PNG images.

## Installation
Clone the repository and install the dependencies:

pip install PyMuPDF

or

```bash
pip install -r requirements.txt
